import * as React from 'react';
import { Color, Size } from "./utils/TypeDefinition";

export interface Props<T> extends React.DOMAttributes<T>{
    type: 'info' | 'success' | 'warning' | 'error';
    className?: string;
    closable?: boolean;
    closeLabel?: string;
    title?: React.ReactNode;
    description?: React.ReactNode;
    showIcon?: boolean;
    full?: boolean;
    classPrefix?: string;
}
export default class Message extends React.Component<Props<Message>>{}