
	import * as React from 'react';
	import { Color, Size } from './utils/TypeDefinition';
	
	export interface Props<T> extends React.DOMAttributes<T> {
	  appearance?: 'default' | 'primary' | 'link' | 'subtle' | 'ghost';
	  classPrefix?: string;
	  componentClass?: React.ReactType;
	  className?: string;
	  active?: boolean;
	  block?: boolean;
	  href?: string;
	  loading?: boolean;
	  disabled?: boolean;
	  size?: Size;
	  color?: Color;
	  // children?: React.ReactNode;
	}
	
	export default class Button extends React.Component<Props<Button>> {}